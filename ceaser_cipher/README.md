# Implementing the Ceaser Cipher encryption. 
You provide a message and an encode number. The alphabets are shifted to encrypt. The same number can be provided to decode

Simply run python3 ceaser_cipher.py
